import toGeoJson from '../../../utils/to-geo-json';
import { module, test } from 'qunit';

module('Unit | Utility | to geo json');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = toGeoJson();
  assert.ok(result);
});
